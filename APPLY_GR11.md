# Apply Growth Release 11

Copy everything in this patch into the root of your real GitHub project:

`C:\Users\Sagar Gohil\Documents\GitHub\canibringitnow-v2`

Choose **Replace the files in the destination** when Windows asks.

Then run:

```powershell
npm run build
npm run dev
```

Test the port shown in the terminal:

- `/rules/power-bank-ryanair/`
- `/airline-guides/easyjet/`
- `/items/power-bank/`
- `/airlines/ryanair/`
- `/countries/india/`
- `/categories/batteries/`
- `/sitemap.xml`

Look for new sections titled **Related rules for this item**, **Explore connected guides**, or **Continue exploring this topic**.

Do not commit if the build fails. If it passes, commit in GitHub Desktop as:

`Growth Release 11 automatic internal linking engine`
