# Release 6.2 — What-If Trip Simulator

Adds a non-destructive simulator to the Trip Checker. Visitors can compare another airline, destination or bag type, temporarily remove items, see a before/after readiness score, and apply or discard the simulated changes.

Analytics events:
- `trip_simulator_opened`
- `trip_simulation_applied`
