import Orbit2Hero from '@/components/orbit2/Orbit2Hero';
import Orbit2CardGrid from '@/components/orbit2/Orbit2CardGrid';
import { orbitLanguages } from '@/lib/orbit2Engine';
export const metadata = { title: 'International SEO Plan | Can I Bring It Now', description: 'Language expansion plan with translation-ready architecture and hreflang priorities.', alternates: { canonical: '/orbit-international/' } };
export default function OrbitInternationalPage() { const cards = orbitLanguages.map((language) => ({ title: `${language.name} (${language.nativeName})`, href: `/orbit-international/${language.code}/`, label: language.priority, description: `Markets: ${language.markets.join(', ')}. Prepare language-specific URLs and human-reviewed priority pages.` })); return <main className="min-h-screen bg-slate-50 pb-24 md:pb-0"><section className="bg-gradient-to-br from-brand-50 via-white to-sky-50"><div className="mx-auto max-w-6xl px-5 py-10 md:px-8"><a href="/orbit-release-2/" className="text-sm font-semibold text-brand-600">← ORBIT Release 2</a><Orbit2Hero eyebrow="Project ORBIT International" title="Global language expansion plan" description="Do not force translation by IP. Build language-specific URLs, suggest browser-language versions and review the highest-value pages first." /><Orbit2CardGrid title="Language rollout priorities" eyebrow="Global SEO" cards={cards} /></div></section></main>; }
