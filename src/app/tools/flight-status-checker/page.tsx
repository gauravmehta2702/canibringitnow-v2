import ToolHero from '@/components/orbit5/ToolHero';
import ToolCardGrid from '@/components/orbit5/ToolCardGrid';
import { getToolInternalLinks } from '@/lib/orbit5Tools';
export const metadata = { title: 'Flight Status Checker | Can I Bring It Now', description: 'Flight status checker framework for future live flight API integration.', alternates: { canonical: '/tools/flight-status-checker/' } };
export default function FlightStatusCheckerPage() { return <main className="min-h-screen bg-slate-50 pb-24 md:pb-0"><section className="bg-gradient-to-br from-brand-50 via-white to-sky-50"><div className="mx-auto max-w-6xl px-5 py-10 md:px-8"><a href="/travel-tools/" className="text-sm font-semibold text-brand-600">← Travel tools</a><ToolHero eyebrow="API-ready" title="Flight Status Checker" description="This is the future live flight status module. It is ready for API integration later, but for now keeps the travel dashboard architecture in place." /><section className="mt-8 rounded-3xl bg-white p-6 shadow-soft ring-1 ring-slate-200"><p className="font-bold text-brand-600">Future integration</p><h2 className="mt-2 text-2xl font-black text-slate-950">What this will support later</h2><div className="mt-5 grid gap-3 md:grid-cols-3">{['Flight number lookup','Departure and arrival status','Delay alerts','Terminal and gate info','Airport weather context','Hotel and transport suggestions'].map((item) => <div key={item} className="rounded-2xl bg-slate-50 p-4 font-bold text-slate-700 ring-1 ring-slate-200">{item}</div>)}</div></section><ToolCardGrid title="Continue planning" eyebrow="Related checks" cards={getToolInternalLinks()} /></div></section></main>; }
