import type { Orbit3Trend } from '@/lib/orbit3Engine';
export default function Orbit3TrendGrid({ title, eyebrow, trends }: { title: string; eyebrow: string; trends: Orbit3Trend[] }) { return (<section className="mt-8 rounded-3xl bg-white p-6 shadow-soft ring-1 ring-slate-200"><p className="font-bold text-brand-600">{eyebrow}</p><h2 className="mt-2 text-2xl font-black text-slate-950">{title}</h2><div className="mt-5 grid gap-3 md:grid-cols-2">{trends.map((trend)=>(<a key={`${trend.href}-${trend.title}`} href={trend.href} className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200 hover:bg-brand-50"><p className="text-xs font-black uppercase tracking-wide text-brand-600">{trend.priority} · {trend.label}</p><p className="mt-1 font-black text-slate-950">{trend.title}</p><p className="mt-2 text-sm leading-6 text-slate-600">{trend.description}</p><p className="mt-3 rounded-xl bg-white p-3 text-xs font-bold leading-5 text-slate-500 ring-1 ring-slate-200">{trend.reason}</p></a>))}</div></section>);}
