'use client';
import { useMemo, useState } from 'react';
export default function AirportDelayReadinessChecker() {
  const [hoursBefore, setHoursBefore] = useState(3), [hasBaby, setHasBaby] = useState(true), [longHaul, setLongHaul] = useState(true);
  const readiness = useMemo(() => { let score = 50; if (hoursBefore >= 3) score += 20; if (longHaul) score += 10; if (hasBaby) score -= 5; return Math.max(0, Math.min(100, score)); }, [hoursBefore, hasBaby, longHaul]);
  return <section className="mt-8 rounded-3xl bg-white p-6 shadow-soft ring-1 ring-slate-200"><h2 className="text-2xl font-black text-slate-950">Airport delay readiness checker</h2><div className="mt-5 grid gap-4 md:grid-cols-3"><label><span className="text-sm font-bold text-slate-600">Hours before flight at airport</span><input type="number" value={hoursBefore} onChange={(e) => setHoursBefore(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 font-bold outline-none focus:border-brand-500" /></label><label className="rounded-2xl bg-slate-50 p-4 font-bold ring-1 ring-slate-200"><input type="checkbox" checked={hasBaby} onChange={(e) => setHasBaby(e.target.checked)} className="mr-2" /> Travelling with baby/child</label><label className="rounded-2xl bg-slate-50 p-4 font-bold ring-1 ring-slate-200"><input type="checkbox" checked={longHaul} onChange={(e) => setLongHaul(e.target.checked)} className="mr-2" /> Long-haul flight</label></div><div className="mt-6 rounded-3xl bg-slate-950 p-6 text-white"><p className="text-sm text-slate-300">Readiness score</p><p className="mt-2 text-5xl font-black">{readiness}/100</p><p className="mt-2 text-slate-300">Bring snacks, chargers, medication, baby items and entertainment if delays are possible.</p></div></section>;
}
