'use client';
import { useMemo, useState } from 'react';
export default function BaggageAllowanceCalculator() {
  const [travellers, setTravellers] = useState(2), [checkedBags, setCheckedBags] = useState(1), [cabinBags, setCabinBags] = useState(2), [checkedWeight, setCheckedWeight] = useState(23), [cabinWeight, setCabinWeight] = useState(7);
  const result = useMemo(() => ({ totalChecked: checkedBags * checkedWeight, totalCabin: cabinBags * cabinWeight, perTravellerChecked: travellers ? Math.round((checkedBags * checkedWeight) / travellers) : 0, perTravellerCabin: travellers ? Math.round((cabinBags * cabinWeight) / travellers) : 0 }), [travellers, checkedBags, cabinBags, checkedWeight, cabinWeight]);
  const fields = [['Travellers', travellers, setTravellers], ['Checked bags', checkedBags, setCheckedBags], ['Cabin bags', cabinBags, setCabinBags], ['Kg per checked bag', checkedWeight, setCheckedWeight], ['Kg per cabin bag', cabinWeight, setCabinWeight]] as const;
  return <section className="mt-8 rounded-3xl bg-white p-6 shadow-soft ring-1 ring-slate-200"><h2 className="text-2xl font-black text-slate-950">Baggage allowance estimate</h2><div className="mt-5 grid gap-4 md:grid-cols-5">{fields.map(([label, value, setter]) => <label key={label} className="block"><span className="text-sm font-bold text-slate-600">{label}</span><input type="number" min="0" value={value} onChange={(e) => setter(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 font-bold outline-none focus:border-brand-500" /></label>)}</div><div className="mt-6 grid gap-3 md:grid-cols-4"><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Total checked</p><p className="text-3xl font-black">{result.totalChecked}kg</p></div><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Total cabin</p><p className="text-3xl font-black">{result.totalCabin}kg</p></div><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Checked / traveller</p><p className="text-3xl font-black">{result.perTravellerChecked}kg</p></div><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Cabin / traveller</p><p className="text-3xl font-black">{result.perTravellerCabin}kg</p></div></div><p className="mt-5 rounded-2xl bg-amber-50 p-4 text-sm leading-6 text-amber-900 ring-1 ring-amber-100">Always confirm exact baggage allowance with your airline and ticket type before flying.</p></section>;
}
