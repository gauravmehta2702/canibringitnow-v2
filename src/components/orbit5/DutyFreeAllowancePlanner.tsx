'use client';
import { useMemo, useState } from 'react';
export default function DutyFreeAllowancePlanner() {
  const [destination, setDestination] = useState('UK'), [goods, setGoods] = useState('perfume, gifts, electronics');
  const checks = useMemo(() => { const lower = `${destination} ${goods}`.toLowerCase(); const list = ['Check destination customs allowance', 'Keep receipts for high-value goods', 'Declare items if required']; if (lower.includes('alcohol')) list.push('Check alcohol litre limits and age rules'); if (lower.includes('tobacco') || lower.includes('cigarette')) list.push('Check tobacco quantity limits'); if (lower.includes('electronics')) list.push('Check high-value electronics declaration rules'); if (lower.includes('food')) list.push('Food and animal-product rules can be strict'); return list; }, [destination, goods]);
  return <section className="mt-8 rounded-3xl bg-white p-6 shadow-soft ring-1 ring-slate-200"><h2 className="text-2xl font-black text-slate-950">Duty-free allowance planner</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><label><span className="text-sm font-bold text-slate-600">Destination</span><input value={destination} onChange={(e) => setDestination(e.target.value)} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 font-bold outline-none focus:border-brand-500" /></label><label><span className="text-sm font-bold text-slate-600">Goods</span><input value={goods} onChange={(e) => setGoods(e.target.value)} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 font-bold outline-none focus:border-brand-500" /></label></div><div className="mt-6 grid gap-3 md:grid-cols-2">{checks.map((check) => <div key={check} className="rounded-2xl bg-slate-50 p-4 font-bold text-slate-700 ring-1 ring-slate-200">☐ {check}</div>)}</div></section>;
}
