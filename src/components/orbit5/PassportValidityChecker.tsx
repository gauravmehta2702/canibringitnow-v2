'use client';
import { useMemo, useState } from 'react';
export default function PassportValidityChecker() {
  const [monthsLeft, setMonthsLeft] = useState(8), [tripMonthsAway, setTripMonthsAway] = useState(1);
  const result = useMemo(() => { const remainingAtTravel = monthsLeft - tripMonthsAway; const status = remainingAtTravel >= 6 ? 'Looks safer' : remainingAtTravel >= 3 ? 'Check destination rules' : 'Renew urgently'; return { remainingAtTravel, status }; }, [monthsLeft, tripMonthsAway]);
  return <section className="mt-8 rounded-3xl bg-white p-6 shadow-soft ring-1 ring-slate-200"><h2 className="text-2xl font-black text-slate-950">Passport validity checker</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><label><span className="text-sm font-bold text-slate-600">Months left on passport today</span><input type="number" value={monthsLeft} onChange={(e) => setMonthsLeft(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 font-bold outline-none focus:border-brand-500" /></label><label><span className="text-sm font-bold text-slate-600">Months until travel</span><input type="number" value={tripMonthsAway} onChange={(e) => setTripMonthsAway(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 font-bold outline-none focus:border-brand-500" /></label></div><div className="mt-6 rounded-3xl bg-slate-950 p-6 text-white"><p className="text-sm text-slate-300">Estimated validity at travel</p><p className="mt-2 text-4xl font-black">{result.remainingAtTravel} months</p><p className="mt-2 text-xl font-bold text-sky-300">{result.status}</p></div><p className="mt-5 rounded-2xl bg-amber-50 p-4 text-sm leading-6 text-amber-900 ring-1 ring-amber-100">Many countries require 3-6 months passport validity. Always check official destination rules.</p></section>;
}
