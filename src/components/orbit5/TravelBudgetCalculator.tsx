'use client';
import { useMemo, useState } from 'react';
export default function TravelBudgetCalculator() {
  const [travellers, setTravellers] = useState(2), [days, setDays] = useState(7), [flights, setFlights] = useState(600), [hotelPerNight, setHotelPerNight] = useState(90), [foodPerDay, setFoodPerDay] = useState(35), [extras, setExtras] = useState(250);
  const result = useMemo(() => { const hotel = hotelPerNight * days; const food = foodPerDay * days * travellers; const total = flights + hotel + food + extras; return { hotel, food, total, perTraveller: travellers ? Math.round(total / travellers) : total }; }, [travellers, days, flights, hotelPerNight, foodPerDay, extras]);
  const fields = [['Travellers', travellers, setTravellers], ['Days', days, setDays], ['Flights total', flights, setFlights], ['Hotel per night', hotelPerNight, setHotelPerNight], ['Food per traveller/day', foodPerDay, setFoodPerDay], ['Extras', extras, setExtras]] as const;
  return <section className="mt-8 rounded-3xl bg-white p-6 shadow-soft ring-1 ring-slate-200"><h2 className="text-2xl font-black text-slate-950">Travel budget calculator</h2><div className="mt-5 grid gap-4 md:grid-cols-3">{fields.map(([label, value, setter]) => <label key={label}><span className="text-sm font-bold text-slate-600">{label}</span><input type="number" min="0" value={value} onChange={(e) => setter(Number(e.target.value))} className="mt-2 w-full rounded-2xl border border-slate-200 bg-slate-50 p-4 font-bold outline-none focus:border-brand-500" /></label>)}</div><div className="mt-6 grid gap-3 md:grid-cols-4"><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Hotel</p><p className="text-3xl font-black">£{result.hotel}</p></div><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Food</p><p className="text-3xl font-black">£{result.food}</p></div><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Total</p><p className="text-3xl font-black">£{result.total}</p></div><div className="rounded-2xl bg-slate-50 p-4 ring-1 ring-slate-200"><p className="text-sm text-slate-500">Per traveller</p><p className="text-3xl font-black">£{result.perTraveller}</p></div></div></section>;
}
